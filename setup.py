from setuptools import setup

setup(name='qml',
      version='0.1',
      description='A library to build quantum convolutional neural networks',
      author='Jay Soni, Ivan Sharankov',
      packages=['qml'],
      license='MIT', install_requires=['numpy', 'matplotlib']
      )